# Oracle EBS 兼容方案 - 技术分析

## 问题本质

```
Oracle EBS → Java Applet (NPAPI) → 浏览器 → JRE 6u7+ → x86/ARM硬件
```

**核心矛盾：**
- JRE 6u7~8 为 x86 架构，ARM64 无法原生运行
- 现代浏览器已移除 NPAPI 插件支持（Chrome ≥45, Firefox ≥57）
- Oracle EBS 依赖 Java Applet，必须通过 NPAPI 加载

## 架构差异

| 维度 | ARM64 | X86/C86 |
|------|-------|---------|
| JRE运行 | 需要模拟层 | 原生运行 |
| 浏览器 | 模拟环境内运行 | 直接安装 |
| 性能 | 有损耗(模拟) | 零损耗 |
| 复杂度 | 高 | 低 |
| 推荐方案 | QEMU虚拟机 | Firefox 52 ESR + OpenJDK 8 |

---

## 浏览器NPAPI支持矩阵

| 浏览器 | NPAPI Java支持 | 版本范围 | 官方状态 | 推荐 | 证据 |
|--------|---------------|---------|---------|------|------|
| **Firefox 52 ESR** | ✅ | 52.0 ~ 52.9.0 | 最后官方支持 | ⭐⭐⭐⭐⭐ | 历史事实 |
| **Pale Moon** | ✅ **持续支持** | 全部版本 (34+仅x86_64) | 官方技术细节明确保留并维护完整NPAPI | ⭐⭐⭐⭐ | [Technical Details](https://www.palemoon.org/technical.shtml): "Full and ongoing support for NPAPI plugins (Java, Flash, Silverlight, etc.)" |
| **SeaMonkey 2.49.5** | ✅ | ≤2.49.5 | 最后支持"非Flash NPAPI（含Java）"的SeaMonkey | ⭐⭐ | [Legacy页面](https://www.seamonkey-project.org/releases/legacy): "The last version supporting plugins other than Flash on Windows and Linux was SeaMonkey 2.49.5." |
| **SeaMonkey 2.53.5 ~ 2.53.24** | ❌ **已全部移除Java NPAPI** | ≥2.53.5 | 官方RN明确移除 | 不可用 | [2.53.18 Release Notes](https://www.seamonkey-project.org/releases/seamonkey2.53.18/): "Support for all NPAPI plugins like Flash, Java and Silverlight has been removed" |
| **Firefox ESR (新)** | ❌ | ≥ 57 | 2017年11月移除NPAPI | 不可用 | — |
| **Chrome/Chromium** | ❌ | ≥ 45 | 2015年9月移除NPAPI | 不可用 | — |
| **Edge** | ❌ | 全部 | 从未支持NPAPI | 不可用 | — |

> **关键事实：** Firefox 在 57 版本（Quantum 项目）中彻底移除了 NPAPI 支持。所有"最新 Firefox ESR"（≥ 57 ESR）都无法加载 Java Applet。只有 Firefox 52 ESR 是最后支持 NPAPI 的官方 Firefox 版本。
>
> **SeaMonkey 2.53.5+ 官方 Release Notes 明确移除了全部 NPAPI（包括 Java），脚本的 x86_plan_c 使用 SeaMonkey 2.49.5（最后支持 Java NPAPI 的版本），并禁止自动升级**。
>
> **Pale Moon 确认：** Pale Moon 34.3.x（2026-08-12 最新版34.3.2.1）官方 Technical Details / 首页 / 开发者文档 三份页面同时声明「Pale Moon 将完整保留并持续维护 NPAPI 和所有基于它的原生插件（含 Java/Flash/Silverlight）」，无需降级。
>
> **通用更正：** Debian/Ubuntu 仓库中的 `firefox-esr` 包版本为 60+（bullseye为78+），均无NPAPI。VM/chroot/Docker等场景**必须**使用 Firefox 52.9.0esr 官方二进制（mozilla FTP 直链可用，已实测校验）。

---

## Java 运行环境选择

### 方案一：Oracle JRE（手动下载）

Oracle JRE 6u7/7/8 是 Oracle EBS 官方推荐的运行时。

**插件文件位置：**
- 32位: `${JRE_HOME}/lib/i386/libnpjp2.so`
- 64位: `${JRE_HOME}/lib/amd64/libnpjp2.so`

**配置步骤：**
1. 从 Oracle 官网下载 JRE tar.gz 或 .bin 安装包
2. 解压到 `/opt/jre`
3. 创建软链接到浏览器插件目录：
   ```bash
   sudo ln -sf /opt/jre/lib/amd64/libnpjp2.so ~/.mozilla/plugins/
   sudo ln -sf /opt/jre/lib/amd64/libnpjp2.so /usr/lib/mozilla/plugins/
   ```
4. Oracle JRE 7/8 需在 Java 控制面板添加 EBS 站点例外

**安装注意事项：**
- `.bin` 自解压包必须以绝对路径执行（`sudo /tmp/jre-6u7-*.bin`），不能用 `./` 拼接
- JRE 6u7 的 `.bin` 解压出 `jre1.6.0_07` 目录，必须将该目录本身作为 `/opt/jre`（而非嵌套进其内部）

### 方案二：OpenJDK 8 + IcedTea（自动安装）⭐ 推荐

OpenJDK 是 JRE 的开源实现，IcedTea-Web 提供 NPAPI Java Applet 支持。

**为什么只推荐 OpenJDK 8？**

| OpenJDK 版本 | IcedTea-Web 兼容性 | Applet 支持 | 说明 |
|-------------|-------------------|------------|------|
| **8** | ✅ 完全兼容 | ✅ 可用 | IcedTea-Web 原生基于 Java 8 |
| 11 | ❌ 不兼容 | ❌ 不可用 | IcedTea-Web 不支持 Java 11+ |
| 17 | ❌ 不兼容 | ❌ 不可用 | 同上 |

**Debian/Ubuntu 安装：**
```bash
sudo apt-get install openjdk-8-jre icedtea-8-plugin
# 或（包名因发行版而异）
sudo apt-get install openjdk-8-jre icedtea-web
```

**RedHat/CentOS 安装：**
```bash
sudo yum install java-1.8.0-openjdk icedtea-web
```

**IcedTea 插件文件位置：**
- Debian系: `/usr/lib/icedtea-web/IcedTeaPlugin.so`
- RedHat系: `/usr/lib/jvm/java-1.8.0-openjdk-*/jre/lib/*/libnpjp2.so`

**优势：**
- 包管理器一键安装，无需手动下载
- IcedTea 插件自动提供 NPAPI 支持
- 无需额外配置 Java 控制面板（Applet 安全提示更少）

---

## X86方案原理

```
X86主机 → Firefox 52 ESR + NPAPI Java插件 → Java运行时 → Oracle EBS
                ↓
    Oracle JRE 6u7/7/8 (手动)  或  OpenJDK 8 + IcedTea (自动)
```

无需任何中间层，直接安装：
1. 安装 Firefox 52 ESR（最后支持NPAPI的官方版）或 Pale Moon（官方持续支持NPAPI）或 SeaMonkey 2.49.5（最后支持Java NPAPI的SeaMonkey版本，禁止升级到2.53+）
2. 安装 Java 运行时（Oracle JRE 或 OpenJDK 8 + IcedTea）
3. 浏览器 Java 插件自动配置（脚本处理软链接）
4. Oracle JRE 7/8 需添加站点例外（OpenJDK 8 通常不需要）

## ARM方案原理

```
ARM64主机 → QEMU/Box64模拟 → x86 Linux → x86 Firefox + x86 Java → Oracle EBS
```

需要x86模拟层：
1. QEMU全系统VM（最稳定，推荐内存≥4GB）
2. QEMU用户模式+proot（轻量级）
3. Box64+Wine（技术探索）
4. Docker+QEMU（容器化）
5. Box86（备选，仅支持32位x86）

---

## JRE版本策略

```
Oracle JRE 6u7 ─→ 直接使用，无安全提示
Oracle JRE 7   ─→ Java控制面板 → 安全 → 站点例外 → 添加EBS地址
Oracle JRE 8   ─→ Java控制面板 → 安全 → 站点例外 → 添加EBS地址
OpenJDK 8      ─→ 通常无需站点例外，IcedTea-Web自动处理
```

**自动配置方式：**

配置必须写入**真实桌面用户**的家目录（sudo运行时 `$HOME=/root` 是错误的，应通过 `SUDO_USER` 定位）：

```bash
mkdir -p ~/.java/deployment
echo "https://ebs.example.com" >> ~/.java/deployment/exception.sites
cat > ~/.java/deployment/deployment.properties << 'EOF'
deployment.security.level=MEDIUM
deployment.security.sandbox.aws.warning.level=allow_unsigned
EOF
```

> 注意：JRE 8u60+ 已废弃 MEDIUM 安全级别，站点例外（exception.sites）是唯一可靠途径。

---

## NPAPI Java插件文件位置

### Oracle JRE
- 32位: `${JRE_HOME}/lib/i386/libnpjp2.so`
- 64位: `${JRE_HOME}/lib/amd64/libnpjp2.so`

### OpenJDK + IcedTea
- `/usr/lib/icedtea-web/IcedTeaPlugin.so`
- `/usr/lib64/icedtea-web/IcedTeaPlugin.so`
- `/usr/lib/jvm/java-8-openjdk-*/jre/lib/*/libnpjp2.so`
- `/usr/lib/jvm/java-1.8.0-openjdk-*/jre/lib/*/libnpjp2.so`

### 浏览器搜索路径
- `~/.mozilla/plugins/libnpjp2.so`
- `/usr/lib/mozilla/plugins/libnpjp2.so`
- `/usr/lib64/mozilla/plugins/libnpjp2.so`

需通过软链接将 Java 插件库复制到浏览器搜索路径。

---

## 统一架构

```
oracle-ebs-setup.sh (单一脚本)
│
├── install_java() ──────── 统一入口
│   ├── [1-3] install_oracle_jre()  ── Oracle JRE 6u7/7/8
│   └── [4]   install_openjdk()     ── OpenJDK 8 + IcedTea
│
├── set_java_env() ───────── 设置 JAVA_HOME 和 PATH
│
├── configure_java_plugin() ──────── Oracle JRE NPAPI插件
├── configure_icedtea_plugin() ───── OpenJDK IcedTea插件 (路径探测)
├── configure_java_control_panel() ─ 站点例外自动配置
│
├── show_arm_plans() ─────────────── ARM64 5个方案
└── show_x86_plans() ─────────────── X86 4个方案
```

---

## 技术要点

1. **sudo 模式**：脚本内所有 `~`、`$HOME`、`cat > 系统目录` 都受运行身份影响，必须通过 `SUDO_USER` 定位真实桌面用户
2. **bash export 陷阱**：`export A=1 B=$A` 中 `$A` 在 export 执行前展开，取到旧值；必须拆为两行
3. **heredoc 变量展开**：需要注入宿主变量的启动脚本用非引用定界符并转义运行时变量；纯静态脚本用引用定界符
4. **仓库浏览器不可用于 Applet**：Debian 10+ 仓库的 firefox-esr 均无 NPAPI，所有场景必须用 Firefox 52 ESR 二进制
5. **set -e 下的管道**：`find | while`、`grep -q | cmd`、`getent | cut` 等管道左侧返回非 0 时整个管道退出码非 0，会触发 `set -e` 终止。解决方案：进程替换 `< <(cmd || true)` 或管道末尾加 `|| true`
6. **文件写入语义**：修改已有配置文件必须用 `>>` 追加，`>` 会覆盖丢失全部已有内容
7. **NPAPI 支持核实**：SeaMonkey 与 Pale Moon 同为 Firefox 分支但走向不同——Pale Moon 官方确认持续保留 NPAPI，SeaMonkey 2.53.5+ 官方 RN 已彻底移除。必须查官方文档，不能凭印象
